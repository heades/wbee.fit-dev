<div class="col">
  <a href="{% if catalog.redirect %}{{ catalog.redirect }}{% else %}{{ catalog.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if catalog.img %}
        {%
          include figure.liquid
          loading="eager"
          path=catalog.img
          sizes = "250px"
          alt="catalog thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ catalog.title }}</h2>
        <p class="card-text">{{ catalog.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if catalog.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ catalog.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if catalog.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ catalog.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
